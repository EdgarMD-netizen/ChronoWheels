"""
Blog application for ChronoWheels.

This module defines the default application configuration using the
``default_app_config`` variable which points to ``BlogConfig``. Django uses
this configuration to initialize the app.
"""

default_app_config = "blog.apps.BlogConfig"